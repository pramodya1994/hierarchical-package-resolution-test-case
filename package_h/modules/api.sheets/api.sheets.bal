# Prints `Hello` with input string name.
#
# + name - the input sting name
# + return - "Hello, " with the input string name

public function sheets(string name) returns string {
    return "wrote to file:" + name;
}
